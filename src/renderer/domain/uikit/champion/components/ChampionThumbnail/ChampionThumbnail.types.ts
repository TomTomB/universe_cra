export interface ChampionAvailabilityProps {
  freeToPlayReward?: boolean;
  freeToPlay?: boolean;
  locked?: boolean;
  rental?: boolean;
}

export interface ChampionThumbnailProps {
  className?: string;
  masteryBanner?: any;
  thumbnail?: any;
  name?: string;
  simple?: boolean;
  freeToPlayReward?: boolean;
  freeToPlay?: boolean;
  locked?: boolean;
  rental?: boolean;
  chestAcquired?: boolean;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const _SNOWPACK = true;
