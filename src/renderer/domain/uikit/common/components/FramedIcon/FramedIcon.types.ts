export interface StyledFramedIconProps {
  zoom?: number;
  interactive?: boolean;
  altFrame?: boolean;
}

export interface FramedIconProps {
  className?: string;
  zoom?: number;
  innerShadow?: boolean;
  altFrame?: boolean;
  interactive?: boolean;
  disabled?: boolean;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const _SNOWPACK = true;
